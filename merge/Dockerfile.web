FROM node:22-slim

RUN apt-get update && apt-get install -y --no-install-recommends git && rm -rf /var/lib/apt/lists/*

WORKDIR /app

# Cache dependencies in image layer
COPY package.json package-lock.json* ./
RUN npm install

COPY . .

EXPOSE 3000 3001

CMD ["node", "server/index.js"]
